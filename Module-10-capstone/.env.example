# SQLite database location, relative to the prisma/ directory.
# See: https://pris.ly/d/connection-strings
DATABASE_URL="file:./dev.db"

# Optional — port the Express server listens on (defaults to 3000).
# PORT=3000

# Optional — controls morgan logging format and test silencing.
# NODE_ENV=development
